# Security constraints
- Make sure all sensitive views are secured with authentication
- Make sure all views have correct authorization (access management)
- Make sure all sensitive methods like login have throttling management
- Make sure we're secure off all injection attacks like xss, sql injection, ...